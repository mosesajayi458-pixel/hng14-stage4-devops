logging:
  volume: swiftdeploy-logs
network:
  driver_type: bridge
  name: swiftdeploy-net
nginx:
  image: nginx:latest
  port: 8080
  proxy_timeout: 5
services:
  image: swift-deploy-1-node:latest
  mode: canary
  port: 3000
  restart_policy: always
  version: 1.0.0
